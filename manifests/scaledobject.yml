apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: nextgenz-cpu-scaledobject
  namespace: nextgenz
spec:
  minReplicaCount: 3
  maxReplicaCount: 6
  scaleTargetRef:
    name: nextgenz-deploy
    kind: Deployment
    apiVersion: apps/v1
  triggers:
  - type: cpu
    metricType: Utilization
    metadata:
      value: "50"
